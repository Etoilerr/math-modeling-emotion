# 术语表格

> 全文术语、符号、单位统一表。论文符号说明表从本表提取"符号、含义、单位"三列。

## 一、中文术语与英文术语对照

| 中文术语 | 英文术语 | 缩写 | 定义 | 禁止混用的近义词 |
|---|---|---|---|---|
| 多模态情感识别 | Multimodal Emotion Recognition | MER | 联合文本、语音、视觉模态预测情感 | 多模态情感分析、多模态情绪识别 |
| 情感极性 | Sentiment Polarity | — | 情感的三分类标签：负向/中性/正向 | 情感类别、情绪极性 |
| 情感强度 | Sentiment Intensity / Sentiment Score | — | 连续值 ∈ [-3, 3]，越接近 ±3 情感越强 | 情感分数、情绪强度 |
| 时序对齐 | Temporal Alignment | — | 将不同采样率的模态特征统一到相同时间位置 | 时间同步、时间对齐 |
| 模态缺失 | Modality Missing / Modality Dropout | — | 某模态内一段连续序列特征全零 | 模态丢失、模态不可用 |
| 有效性掩码 | Validity Mask | — | 标识每个时间步特征是否有效的二值掩码 | 注意力掩码、填充掩码 |
| 局部连续全零 | Local Contiguous All-Zero | — | 缺失的具体形式：连续时间步特征向量全零 | 局部缺失、连续缺失 |
| 片段级重要性 | Fragment-level Importance | — | 模态内每个时间位置对预测的贡献程度 | 时间步重要性、位置权重 |
| 模态作用程度 | Modality Contribution | — | 每个模态对当前样本预测的归一化权重 | 模态权重、模态重要性 |
| 主要参考模态 | Primary Reference Modality | — | 作用程度最大的模态 | 主导模态、关键模态 |
| 遮挡法 | Occlusion / Perturbation | — | 移除输入片段观察预测变化的解释验证方法 | 遮蔽法、扰动法 |
| 门控融合 | Gated Fusion | — | 用可学习门控权重调节各模态贡献的融合方式 | 注意力融合、加权融合 |
| 双任务学习 | Multi-task Learning | — | 分类与回归共享编码器、联合训练 | 联合学习、多目标学习 |
| 对齐位置 | Aligned Position | — | 时序对齐后的离散时间索引（0–49） | 时间步、序列位置 |
| 有效长度 | Valid Length | — | 非零填充的对齐位置数 | 有效步数、序列长度 |

## 二、全局符号表

| 符号 | 含义 | 单位 | 首次出现 |
|---|---|---|---|
| $T$ | 视频原始时长 | 秒 (s) | Q1 时序对齐 |
| $N_w$ | 文本词数 | 个 | Q1 文本特征 |
| $d_m$ | 模态 $m$ 的特征维度 | 维 | Q1/Q2 输入 |
| $d_\text{text}$ | 文本特征维度（BERT 隐藏层大小） | 维 (=768) | Q1 文本特征 |
| $d_\text{audio}$ | 语音特征维度 | 维 (=74) | Q1 语音特征 |
| $d_\text{vision}$ | 视觉特征维度 | 维 (=35) | Q1 视觉特征 |
| $d$ | 融合后隐层维度 | 维 (=128) | Q2 编码器 |
| $\mathbf{X}_m$ | 模态 $m$ 的对齐特征矩阵，形状 $(B, 50, d_m)$ | 无量纲 | Q2 输入 |
| $\mathbf{M}_m$ | 模态 $m$ 的有效性掩码，形状 $(B, 50)$，取值 $\{0,1\}$ | 无量纲 | Q2 掩码 |
| $\mathbf{Z}_m$ | 模态 $m$ 编码后的时序表示，形状 $(B, 50, d)$ | 无量纲 | Q2 编码器 |
| $\bar{\mathbf{z}}_m$ | 模态 $m$ 掩码感知池化后的向量，形状 $(B, d)$ | 无量纲 | Q2 池化 |
| $\mathbf{g}_m$ | 模态 $m$ 的门控权重，形状 $(B, d)$ | 无量纲 | Q2 融合 |
| $\mathbf{h}_\text{fuse}$ | 融合表示向量，形状 $(B, d)$ | 无量纲 | Q2 融合 |
| $\hat{\mathbf{y}}_\text{cls}$ | 分类预测 logits，形状 $(B, 3)$ | 无量纲 | Q2 输出 |
| $\hat{y}_\text{reg}$ | 回归预测值，形状 $(B,)$ | 无量纲 | Q2 输出 |
| $\mathbf{y}_\text{cls}$ | 分类真实标签，取值 $\{0,1,2\}$ | 无量纲 | Q2 损失 |
| $y_\text{reg}$ | 回归真实标签，取值 $[-3, 3]$ | 无量纲 | Q2 损失 |
| $\alpha_m$ | 模态 $m$ 的注意力权重（作用程度），$\sum_m \alpha_m = 1$ | 无量纲 | Q3 模态级解释 |
| $\beta_{m,t}$ | 模态 $m$ 第 $t$ 个位置的片段重要性，$\sum_t \beta_{m,t} = 1$ | 无量纲 | Q3 片段级解释 |
| $\lambda_\text{CE}$ | 分类损失权重 | 无量纲 (=1.0) | Q2 损失 |
| $\lambda_\text{MSE}$ | 回归损失权重 | 无量纲 (=0.5) | Q2 损失 |
| $p_\text{aug}$ | 缺失增强概率 | 无量纲 (=0.7) | Q2 训练 |
| $L$ | 缺失区间长度（对齐位置数） | 个 | Q2 增强 |
| $B$ | 批大小 | 个 (=64) | Q2 训练 |
| $\epsilon$ | 数值稳定小量 | 无量纲 ($10^{-8}$) | Q2 池化 |
| $\mathbf{w}_\text{cls}$ | 分类类别权重向量 | 无量纲 | Q2 损失 |

## 三、下标与上标约定

| 标记 | 含义 |
|---|---|
| $m \in \{\text{text}, \text{audio}, \text{vision}\}$ | 模态索引 |
| $t = 1, \dots, 50$ | 对齐位置索引（论文中从 1 开始计数；代码中从 0 开始） |
| $i = 0, \dots, 49$ | 代码中的对齐位置索引 |
| $j = 1, \dots, N_w$ | 文本词索引 |
| $k$ | 原始帧/特征帧索引 |
| $\hat{\cdot}$ | 模型预测值 |
| $\bar{\cdot}$ | 池化/平均后的量 |

## 四、指标符号

| 符号 | 含义 | 单位 |
|---|---|---|
| Acc | 分类准确率 (Accuracy) | % |
| F1 | 宏平均 F1 值 (macro-F1) | % |
| MAE | 平均绝对误差 (Mean Absolute Error) | 无量纲 |
| $r$ | 皮尔逊相关系数 (Pearson Correlation) | 无量纲 |

## 五、文件与路径术语

| 术语 | 含义 |
|---|---|
| aligned_50 | 附件 2 中对齐到 50 位置的特征文件 |
| unaligned_50 | 附件 2 中未对齐的特征文件（本次不使用） |
| 附件 3 | 模态缺失专项测试集（30 条无标签） |
| 附件 4 | 可解释性专项测试集（20 条无标签 + 原始视频） |

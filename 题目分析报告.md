# 复杂场景下多模态情感预测的数学建模与算法设计——题目分析报告

> 竞赛：第二十三届中国研究生数学建模竞赛 E 题
> 特征版本：aligned_50（50 对齐位置），全程统一
> 数据来源：仅使用赛题提供的 CMU-MOSEI 系列数据，不引入外部情感数据集

---

## 一、题目与附件清单

### 1.1 题目概述

本题以 CMU-MOSEI 英文多模态情感数据集为基础，围绕三个层级开展建模：
1. **问题 1（Q1）**：从 100 条原始视频提取文本、语音、视觉三模态特征并时序对齐到 50 个位置。
2. **问题 2（Q2）**：构建对"局部连续全零缺失"鲁棒的双任务模型（三分类极性 + 连续强度回归），对附件 3 无标签集预测。
3. **问题 3（Q3）**：三模态完整时预测 + 模态级/片段级解释，高贡献片段映射回原始文本/语音时段/视频关键帧，对附件 4 无标签集预测 + 解释。

### 1.2 附件清单

| 附件 | 内容 | 规模 | 用途 |
|---|---|---|---|
| 附件 1 | 原始多模态样本（37 个 video_id 文件夹，含 mp4 片段 + label-100.xlsx） | 100 条视频，时长 2.648–34.567 s | Q1 特征提取 |
| 附件 2 | 标准化特征文件 aligned_50.pkl / unaligned_50.pkl + label.xlsx | 各 4850 条（train 3395 / valid 728 / test 727） | Q2、Q3 训练验证 |
| 附件 3 | 模态局部缺失专项测试集（对齐版本 30 个 pkl） | 30 条无标签，含随机连续全零缺失 | Q2 最终推理 |
| 附件 4 | 可解释性专项测试集（对齐版本 20 个 pkl + videos/） | 20 条无标签，三模态完整 + 原始视频 | Q3 最终推理 + 解释 |

### 1.3 附件 2 数据结构（aligned_50.pkl）

最外层键：`train` / `valid` / `test`。每个划分下字段集中保存：

| 字段 | 形状 | 类型 | 含义 |
|---|---|---|---|
| `text` | (N, 50, 768) | float32 | BERT 词级嵌入，对齐到 50 位置 |
| `audio` | (N, 50, 74) | float64 | 语音韵律特征，对齐到 50 位置 |
| `vision` | (N, 50, 35) | float64 | 视觉特征，对齐到 50 位置 |
| `text_bert` | (N, 3, 50) | int64 | [input_ids, attention_mask, token_type_ids] |
| `raw_text` | (N,) | str | 原始英文转写文本 |
| `classification_labels` | (N,) | float64 | 0=Negative, 1=Neutral, 2=Positive |
| `regression_labels` | (N,) | float64 | 连续情感强度 ∈ [-3, 3] |
| `id` | list[N] | str | 样本标识 |

**标签分布**：train 负 967 / 中 758 / 正 1670；valid 负 206 / 中 184 / 正 338；test 负 207 / 中 158 / 正 362。正向样本偏多，需在损失或评估中关注类别不平衡。

**填充模式**：text 无全零时间步（BERT 嵌入始终非零）；audio 和 vision 存在大量全零时间步（尾部填充），train 中 audio 全零步 92933/169750、vision 全零步 97117/169750。模型必须用有效性掩码区分"真零特征"与"填充/缺失零"。

### 1.4 附件 3 数据结构

每个 pkl 含 `test` 键，其下字段与附件 2 一致（text/audio/vision/text_bert/raw_text/id），但无标签。模态缺失表现为一个或多个模态中存在特征值全部为零的随机连续序列区间。

### 1.5 附件 4 数据结构

每个 pkl 含 `test` 键，字段与附件 2 一致，三模态信息完整（无模拟缺失）。配套 videos/ 目录下 01–20.mp4 用于将关键证据时间位置回看至原始片段。

---

## 二、子问题拆解

### Q1：多模态情感特征提取与时序对齐

- **目标**：从附件 1 的 100 条原始视频中，独立提取文本、语音、视觉三模态特征，统一对齐到 50 个时间位置，输出可追溯、可复核的特征文件。
- **输入**：原始 mp4 视频 + label-100.xlsx（含 video_id, clip_id, text, label, annotation）。
- **输出**：
  - 全量对齐特征文件（text: 100×50×768, audio: 100×50×74, vision: 100×50×35）
  - 100 条样本汇总表（样本编号、模态类型、原始有效时长、特征维度、对齐粒度、有效长度）
  - 逐位置映射表（对齐位置 → 时间区间 → 文本片段 → 语音帧范围 → 视频帧范围 → 特征范数）
  - 至少 1 个典型样本的时序对齐验证
- **评价口径**：覆盖完整性（100/100）、时序可核验性（位置/有效长度/填充规则可追溯）、方法可复现性（工具/参数/日志）。

### Q2：模态信息缺失条件下的情感预测建模与验证

- **目标**：构建对局部连续全零缺失鲁棒的多模态情感预测模型，同时输出情感极性（三分类）和情感强度（连续回归），分析缺失模态类型、缺失位置、缺失时长对性能的影响，对附件 3 完成推理。
- **输入**：附件 2 aligned_50.pkl（train/valid/test），附件 3（30 条无标签）。
- **输出**：
  - 模型参数文件 + 配置
  - 验证集/测试集指标（Accuracy, macro-F1, MAE, Pearson）
  - 缺失消融实验结果（按缺失模态类型 × 缺失率分组的性能变化）
  - 附件 3 全量预测 CSV（样本编号、极性预测、强度预测）
  - 训练日志
- **评价口径**：分类 Accuracy / macro-F1；回归 MAE / Pearson。训练集学习参数，验证集选模型结构/超参/阈值，附件 3 仅最终推理。

### Q3：可解释性情感预测建模与验证

- **目标**：在三模态完整条件下，构建可解释性多模态情感预测模型，输出预测结果 + 模态作用程度 + 主要参考模态 + 关键证据片段定位，关键证据可映射回原始文本/语音/视觉，对附件 4 完成推理 + 解释。
- **输入**：附件 2 aligned_50.pkl（train/valid/test），附件 4（20 条无标签 + 原始视频）。
- **输出**：
  - 模型参数文件 + 配置
  - 验证集/测试集指标
  - 典型样本解释卡（预测结果、三模态作用程度、主要参考模态、关键证据定位）
  - 片段级重要性热力图（模态内局部片段重要性分布）
  - 遮挡法合理性验证（top-K vs bottom-K 移除扰动对比）
  - 附件 4 全量预测 + 解释 CSV
- **评价口径**：同 Q2 指标 + 解释可复核性（关键证据可映射回原始素材）。

---

## 三、数据理解与预处理计划

### 3.1 附件 1 原始视频

- 37 个 video_id 文件夹，每个含 1–多个 clip_id.mp4，共 100 条样本。
- label-100.xlsx 提供 video_id, clip_id, text（英文转写）, label（连续强度）, annotation（极性文字标签）。
- 视频时长差异大（2.648–34.567 s），需统一时间轴后对齐。
- 文本无逐词时间戳，需按句序均匀映射到视频时长。
- label-100.xlsx 中连续强度实际范围为 [-2.0, 2.67]（annotation: Positive 57 / Neutral 25 / Negative 18），与附件 2 回归标签范围 [-3, 3] 不同；Q1 仅做特征提取不涉及标签训练，Q2/Q3 使用附件 2 标签。

### 3.2 附件 2 特征

- 已对齐到 50 位置，text 维度 768（BERT），audio 74，vision 35。
- audio/vision 尾部全零为填充，需构造有效性掩码：$\mathbf{M}_m[t] = 1$ 当且仅当该时间步特征向量的 L1 范数 > 0。
- text 无全零步，但其 attention_mask（text_bert 第 2 行）可标识真实 token 位置。
- 标签中 0 仅属中性，不归入负向或正向；分类标签 0/1/2 对应 Negative/Neutral/Positive。

### 3.3 附件 3 缺失特征

- 缺失定义：某模态内一段连续序列全零（不是整模态消失）。
- 与附件 2 的尾部填充不同，附件 3 的缺失可能出现在序列任意位置。
- 推理时需用同一掩码规则识别缺失位置，模型应显式利用掩码。

### 3.4 预处理统一规则

1. **标准化**：对 train 集各模态特征计算均值/标准差，对 train/valid/test/附件3/附件4 统一应用（仅用 train 统计量，避免数据泄露）。
2. **有效性掩码**：$\mathbf{M}_m[t] = \mathbb{1}(\|\mathbf{x}_m[t]\|_1 > 0)$，$m \in \{\text{text}, \text{audio}, \text{vision}\}$。
3. **缺失增强**（Q2 训练）：以概率 p 对随机模态的随机连续区间 [s, s+L) 置零并同步置掩码，L ∈ [0.1T, 0.5T]。
4. **类别不平衡**：分类损失使用类别加权（权重与 train 各类频率成反比）。

---

## 四、假设及依据

### 关键假设（放宽会导致模型失效）

| 编号 | 假设 | 依据 | 放宽后果 |
|---|---|---|---|
| A1 | 附件 2 的 aligned_50 特征中，audio/vision 的全零时间步为填充而非真实零特征 | 数据分析：全零步集中在尾部且与有效长度相关；text 无全零步 | 若全零为真实特征，掩码会错误屏蔽有效信息，模型输入失真 |
| A2 | 附件 3 的缺失区间可通过"连续全零"模式与填充区分，且模型掩码规则适用于附件 3 | 题目定义缺失为"特征值全部为零的随机连续序列区间" | 若缺失模式不可检测，模型无法区分缺失与填充，鲁棒性失效 |
| A3 | 仅用赛题数据训练，不引入外部情感数据集 | 题目补充说明第 1 条明确禁止 | 违反竞赛规则 |

### 可放宽假设（放宽只影响精度，需复验）

| 编号 | 假设 | 依据 | 放宽后复验方式 |
|---|---|---|---|
| B1 | 文本无逐词时间戳时，按词在句中的顺序均匀映射到视频时长 | 附件 1 未提供逐词时间戳，均匀映射是最简无偏假设 | 灵敏度分析：对比均匀映射 vs 按词长加权映射 |
| B2 | 语音特征采用 74 维（MFCC 20 + Δ 20 + Δ² 20 + chroma 12 + RMS 1 + centroid 1），与附件 2 维度一致 | 附件 2 audio 维度为 74，需保持接口兼容；该分解仅适用于 Q1 自提取特征（见 §5.1.3 声明） | 对比不同特征组合的下游分类性能 |
| B3 | 视觉特征采用 35 维（HSV 直方图 32 + 灰度统计 3），5 fps 抽帧 | 附件 2 vision 维度为 35；5 fps 在时长范围内可覆盖；该分解仅适用于 Q1 自提取特征（见 §5.1.3 声明） | 对比不同抽帧率（3/5/10 fps）的特征稳定性 |
| B4 | 对齐粒度为 50 个均匀时间位置 | 附件 2 文件名 aligned_50 明确 50 位置 | 对比 25/50/100 位置的信息保留率 |
| B5 | 缺失增强区间长度比例为 [0.1, 0.5] | 覆盖轻度到中度缺失，与附件 3 预期缺失率匹配 | 扫描不同缺失率区间的验证集性能 |
| B6 | 融合维度 d_model = 128 | 在模型容量与过拟合间取平衡 | 验证集上扫描 d_model ∈ {64, 128, 256} |
| B7 | 分类与回归损失权重 λ_CE=1.0, λ_MSE=0.5 | 回归标签范围 [-3,3]，MSE 量级小于 CE | 验证集上扫描权重比 |

---

## 五、每题模型与数学定义

### 5.1 Q1：三模态特征提取与时序对齐模型

#### 5.1.1 文本特征

使用 bert-base-uncased 对转写文本编码，取最后一层隐藏状态，按词聚合子 token 嵌入：

$$\mathbf{h}_j = \frac{1}{|S_j|}\sum_{i \in S_j} \mathbf{e}_i, \quad j = 1, \dots, N_w$$

其中 $S_j$ 为第 $j$ 个词对应的子 token 索引集合，$\mathbf{e}_i \in \mathbb{R}^{768}$ 为 BERT 输出。

由于无逐词时间戳，第 $j$ 个词的时间区间均匀映射为：

$$[\tau_j^\text{start}, \tau_j^\text{end}) = \left[\frac{j-1}{N_w}T, \frac{j}{N_w}T\right)$$

其中 $T$ 为视频时长（由 ffmpeg 探测）。

#### 5.1.2 语音特征

16 kHz 单声道重采样后提取：
- MFCC：20 维（n_fft=2048, hop_length=512）
- Δ-MFCC：20 维
- Δ²-MFCC：20 维
- Chroma STFT：12 维
- RMS：1 维
- Spectral Centroid：1 维

合计 74 维，帧移 32 ms。帧时间戳由 librosa.frames_to_time 给出。

#### 5.1.3 视觉特征

5 fps 均匀抽帧，每帧提取：
- HSV 直方图：H 16 bin + S 8 bin + V 8 bin = 32 维（归一化）
- 灰度统计：均值 + 标准差 + 对比度 = 3 维

合计 35 维。帧时间戳由 OpenCV CAP_PROP_POS_MSEC 给出。

> **声明**：上述 74 维（语音）与 35 维（视觉）的维度分解仅适用于 Q1 自提取特征的定义，用于保证自生成特征文件与附件 2 接口维度一致。题目补充说明明确"语音特征维度 74 由原始提取流程确定，本赛题将其视为统一输入维度，不对其每一维的具体物理含义作额外假定"，因此本报告不对附件 2 已有 74/35 维特征的物理含义作任何还原或断言。

#### 5.1.4 时序对齐

将三模态帧级特征统一池化到 50 个位置。位置 $i$ 对应时间区间：

$$\left[\frac{i}{50}T, \frac{i+1}{50}T\right), \quad i = 0, 1, \dots, 49$$

对每个位置，取落入该区间的原始帧/词的特征均值：

$$\mathbf{x}_m[i] = \frac{1}{|\mathcal{I}_{m,i}|}\sum_{k \in \mathcal{I}_{m,i}} \mathbf{f}_{m,k}, \quad \mathcal{I}_{m,i} = \{k : \tau_k^\text{start} < \tfrac{i+1}{50}T \land \tau_k^\text{end} \ge \tfrac{i}{50}T\}$$

无原始证据的位置填充零向量。有效长度为非零位置数。

#### 5.1.5 模型合同

| 项目 | 内容 |
|---|---|
| 任务定义 | 从 100 条原始视频提取三模态特征并对齐到 50 位置 |
| 数据 | 附件 1 视频 + label-100.xlsx |
| 结论类型 | 特征文件 + 汇总表 + 映射表 + 典型样本验证 |
| 模型 | 单阶段特征提取管线（BERT + librosa + OpenCV + 均匀池化对齐） |
| 数学定义 | 见 5.1.1–5.1.4 |
| 实现 | Python，依赖 torch/transformers/librosa/opencv-python/imageio-ffmpeg |
| 验证 | 100/100 覆盖、维度校验、典型样本三模态时间轴对齐可视化 |
| 风险 | 极短视频有效位置过少；BERT 子词聚合边界；ffmpeg 时长探测失败回退 |

---

### 5.2 Q2：模态缺失鲁棒双任务预测模型

#### 5.2.1 模型架构：Mask-Aware Multimodal Fusion Network (MA-MFN)

**输入**：三模态序列 $\mathbf{X}_m \in \mathbb{R}^{B \times 50 \times d_m}$（$d_\text{text}=768, d_\text{audio}=74, d_\text{vision}=35$）及有效性掩码 $\mathbf{M}_m \in \{0,1\}^{B \times 50}$。

**单模态时序编码器**：

$$\mathbf{Z}_m = \text{GELU}(\text{LayerNorm}(\mathbf{W}_m \mathbf{X}_m + \mathbf{b}_m))$$
$$\mathbf{Z}_m' = \mathbf{Z}_m + \text{Conv1d}_m(\mathbf{Z}_m^\top)^\top, \quad \text{Conv1d}: \text{depthwise}, k=3, p=1$$
$$\mathbf{Z}_m'' = \text{LayerNorm}(\mathbf{Z}_m'), \quad \mathbf{Z}_m'' \in \mathbb{R}^{B \times 50 \times d}$$

**掩码感知时序池化**：

$$\bar{\mathbf{z}}_m = \frac{\sum_{t=1}^{50} M_m[t] \cdot \mathbf{Z}_m''[t]}{\max(\sum_{t=1}^{50} M_m[t], \epsilon)}, \quad \bar{\mathbf{z}}_m \in \mathbb{R}^{B \times d}$$

**门控跨模态融合**：

$$\mathbf{g}_m = \sigma(\mathbf{W}_{g,m} [\bar{\mathbf{z}}_\text{text}; \bar{\mathbf{z}}_\text{audio}; \bar{\mathbf{z}}_\text{vision}] + \mathbf{b}_{g,m})$$
$$\mathbf{h}_\text{fuse} = \text{LayerNorm}(\text{GELU}(\mathbf{W}_f [\mathbf{g}_\text{text} \odot \bar{\mathbf{z}}_\text{text}; \mathbf{g}_\text{audio} \odot \bar{\mathbf{z}}_\text{audio}; \mathbf{g}_\text{vision} \odot \bar{\mathbf{z}}_\text{vision}] + \mathbf{b}_f))$$

**双任务头**：

$$\hat{\mathbf{y}}_\text{cls} = \text{ClsHead}(\mathbf{h}_\text{fuse}) \in \mathbb{R}^{B \times 3}, \quad \hat{y}_\text{reg} = \text{RegHead}(\mathbf{h}_\text{fuse}) \in \mathbb{R}^{B}$$

#### 5.2.2 目标函数

$$\mathcal{L} = \lambda_\text{CE} \cdot \text{CE}(\hat{\mathbf{y}}_\text{cls}, \mathbf{y}_\text{cls}; \mathbf{w}_\text{cls}) + \lambda_\text{MSE} \cdot \text{MSE}(\hat{y}_\text{reg}, y_\text{reg})$$

其中 $\mathbf{w}_\text{cls}$ 为类别加权（与 train 频率成反比），$\lambda_\text{CE}=1.0, \lambda_\text{MSE}=0.5$。

#### 5.2.3 缺失增强训练

对每个训练样本，以概率 $p_\text{aug}=0.7$ 选择一个模态，随机生成连续缺失区间 $[s, s+L)$：

$$L \sim \text{Uniform}(\lfloor 0.1 \times 50 \rfloor, \lfloor 0.5 \times 50 \rfloor), \quad s \sim \text{Uniform}(0, 50-L)$$

将该模态在 $[s, s+L)$ 的特征置零，掩码同步置零。

#### 5.2.4 缺失消融实验

在验证集上系统构造缺失：
- 缺失模态类型：text / audio / vision / 随机
- 缺失率：10%, 20%, 30%, 40%, 50%
- 缺失位置：头部 / 中部 / 尾部
- 测量各条件下 Accuracy / macro-F1 / MAE / Pearson 的变化

#### 5.2.5 模型合同

| 项目 | 内容 |
|---|---|
| 任务定义 | 局部连续缺失鲁棒的三分类 + 回归双任务预测 |
| 数据 | 附件 2 aligned_50.pkl（train/valid/test），附件 3 推理 |
| 结论类型 | 验证/测试指标 + 缺失消融 + 附件 3 预测 CSV |
| 模型 | MA-MFN（掩码感知编码器 + 门控融合 + 双任务头），单模型体系 |
| 数学定义 | 见 5.2.1–5.2.3 |
| 实现 | PyTorch，CPU 训练，AdamW + CosineAnnealing，batch_size=64，30 epochs |
| 验证 | 验证集选超参，测试集报告最终指标，缺失消融量化鲁棒性 |
| 风险 | CPU 训练慢；类别不平衡；缺失增强与真实缺失分布不匹配 |

---

### 5.3 Q3：可解释性多模态情感预测模型

#### 5.3.1 模型架构：Attention-based Explainable Multimodal Network (A-EMN)

在 MA-MFN 基础上增加可解释性组件，共享单模态编码器和融合层。

**模态级注意力**：

$$\alpha_m = \frac{\exp(\mathbf{q}^\top \mathbf{W}_q \bar{\mathbf{z}}_m)}{\sum_{m'} \exp(\mathbf{q}^\top \mathbf{W}_q \bar{\mathbf{z}}_{m'})}, \quad m \in \{\text{text}, \text{audio}, \text{vision}\}$$

$\alpha_m$ 为模态 $m$ 的作用程度（归一化权重），主要参考模态为 $\arg\max_m \alpha_m$。

**片段级注意力**（每个模态内）：

$$\beta_{m,t} = \frac{\exp(\mathbf{v}^\top \mathbf{W}_v \mathbf{Z}_m''[t])}{\sum_{t'=1}^{50} \exp(\mathbf{v}^\top \mathbf{W}_v \mathbf{Z}_m''[t'])}, \quad t = 1, \dots, 50$$

$\beta_{m,t}$ 为模态 $m$ 第 $t$ 个位置的片段重要性。关键证据片段为 $\beta_{m,t}$ 最高的 top-K 个连续或离散位置。

**注意力加权池化**（替代简单均值池化）：

$$\tilde{\mathbf{z}}_m = \sum_{t=1}^{50} \beta_{m,t} \cdot M_m[t] \cdot \mathbf{Z}_m''[t]$$

融合与双任务头同 Q2。

#### 5.3.2 关键证据回映

- **文本**：top-K 位置 → 映射表中该位置对应的文本词片段
- **语音**：top-K 位置 → 时间区间 $[iT/50, (i+1)T/50)$ → 语音时段
- **视觉**：top-K 位置 → 时间区间 → 该区间内的视频关键帧（取中间帧）

#### 5.3.3 遮挡法合理性验证

在验证集上：
1. 对每个样本，按 $\beta_{m,t}$ 排序，移除 top-K 片段（置零 + 掩码置零），记录预测变化 $\Delta_\text{top}$
2. 移除 bottom-K 片段，记录预测变化 $\Delta_\text{bottom}$
3. 合理性判据：$\Delta_\text{top} > \Delta_\text{bottom}$（高贡献片段移除应导致更大预测扰动）
4. 统计验证集上满足判据的样本比例，以及平均扰动差异

#### 5.3.4 模型合同

| 项目 | 内容 |
|---|---|
| 任务定义 | 三模态完整时的可解释双任务预测，输出模态/片段级解释 |
| 数据 | 附件 2 aligned_50.pkl（train/valid/test），附件 4 推理 + 原始视频 |
| 结论类型 | 指标 + 解释卡 + 片段热力图 + 遮挡验证 + 附件 4 预测解释 CSV |
| 模型 | A-EMN（MA-MFN + 模态/片段级注意力），与 Q2 同模型族的可解释变体 |
| 数学定义 | 见 5.3.1–5.3.3 |
| 实现 | PyTorch，与 Q2 共享编码器初始化，注意力层新增训练 |
| 验证 | 同 Q2 指标 + 遮挡法合理性验证 + 关键证据可回映性检查 |
| 风险 | 注意力权重不一定等于因果重要性；top-K 选择敏感；极短视频片段数少 |

---

## 六、算法步骤总览

### Q1 流程
1. 读取 label-100.xlsx，遍历 100 条样本
2. ffmpeg 探测视频时长 $T$，抽取 16 kHz 单声道 wav
3. 文本：BERT 编码 → 词级聚合 → 均匀时间映射
4. 语音：librosa 提取 74 维帧级特征
5. 视觉：5 fps 抽帧 → 35 维帧级特征
6. 三模态统一池化到 50 位置
7. 保存 npz + summary.csv + mapping.csv
8. 典型样本对齐验证

### Q2 流程
1. 加载 aligned_50.pkl，构造有效性掩码
2. 标准化（用 train 统计量）
3. 构建 MA-MFN 模型
4. 缺失增强训练（30 epochs，AdamW + CosineAnnealing）
5. 验证集选最佳模型（macro-F1 优先）
6. 测试集评估
7. 缺失消融实验
8. 附件 3 推理 → CSV

### Q3 流程
1. 加载 aligned_50.pkl，构造有效性掩码
2. 构建 A-EMN 模型（共享 Q2 编码器初始化）
3. 训练（无缺失增强，三模态完整）
4. 验证集/测试集评估
5. 提取模态级注意力 $\alpha_m$ 和片段级注意力 $\beta_{m,t}$
6. 遮挡法合理性验证
7. 典型样本解释卡 + 片段热力图
8. 附件 4 推理 + 解释 → CSV，关键帧提取

---

## 七、编程依赖和候选图表

### 7.1 依赖

- Python 3.12.10，torch 2.13.0 (CPU)，transformers 5.17.0，librosa 1.0.0，opencv-python 5.0.0
- scikit-learn 1.9.1，scipy 1.18.0，pandas 3.0.5，numpy 2.5.2，matplotlib 3.11.1
- python-docx 1.2.0（论文生成）
- HF_ENDPOINT=https://hf-mirror.com

### 7.2 候选图表（每类 ≥3 张，覆盖 Q1/Q2/Q3）

**原始数据图（raw）**：
- raw_q1_label_distribution：100 条样本情感强度分布直方图 + 极性饼图
- raw_q1_duration_distribution：视频时长分布直方图
- raw_q2_modality_valid_length：三模态有效长度分布（train/valid/test）

**模型运行过程图（process）**：
- process_q1_alignment_example：典型样本三模态时间轴对齐示意
- process_q2_training_curve：训练损失 + 验证指标曲线
- process_q2_missing_augmentation：缺失增强示意图（原始 vs 缺失后掩码）

**模型最终结果图（result）**：
- result_q1_feature_summary：100 条样本特征维度/有效长度汇总热力图
- result_q2_missing_ablation：缺失模态类型 × 缺失率的性能热力图
- result_q2_confusion_matrix：测试集分类混淆矩阵
- result_q3_modality_weights：验证集三模态注意力权重分布箱线图
- result_q3_fragment_heatmap：典型样本片段级重要性热力图
- result_q3_occlusion_validation：top-K vs bottom-K 移除扰动对比

**建模流程图**：
- flow_overall_model：总体流程图（输入 → Q1/Q2/Q3 依赖 → 模型 → 求解 → 验证 → 输出）

---

## 八、风险与回退条件

| 风险 | 触发条件 | 回退方案 |
|---|---|---|
| BERT 权重下载失败 | HF 镜像不可达 | 使用已缓存的 bert-base-uncased；若缓存损坏，用 text_bert 字段的 input_ids 重新编码 |
| 视频解码失败 | 个别 mp4 损坏 | 跳过该样本并记录，用零特征填充；确保 100 条中 ≥95 条成功 |
| CPU 训练过慢 | 30 epochs > 2 小时 | 减少 epochs 至 15，减小 d_model 至 64，或减小 batch 后用梯度累积 |
| 附件 3 缺失模式不可检测 | 全零区间与填充无法区分 | 统一用 L1 范数阈值掩码；在论文中说明局限性 |
| 注意力解释不稳定 | 不同 run 注意力差异大 | 固定随机种子，多次运行取平均注意力；报告方差 |
| 附件总体积 > 50MB | 特征文件/模型/图过大 | 压缩 npz，模型用 fp16 或 state_dict 仅保存，图片用 PNG 压缩 |

---

## 九、文献依据

以下文献以题目给定的 10 篇参考文献为骨架，按其在建模中的支撑作用归类引用。

### 题目给定参考文献

1. Zhang S, Yang Y, Chen C, et al. Deep learning-based multimodal emotion recognition from audio, visual, and text modalities: A systematic review of recent advancements and future prospects[J]. Expert Systems with Applications, 2024, 237: 121692.
   - 多模态情感识别综述，支撑三模态融合架构选择与评价口径。

2. Qiu X, Fang Y, Zhou Q, et al. Beyond Missing Modalities: Hypergraph Conditioned Diffusion for Uncertainty-Aware Multimodal Emotion Recognition[C]//CVPR. 2026.
   - 模态缺失鲁棒性建模，支撑缺失增强与掩码机制设计。

3. Yang Z, Li M. Factorize, Reconstruct, Enhance: A Unified Framework for Multimodal Sentiment Analysis[C]//CVPR. 2026.
   - 多模态情感分析统一框架，支撑编码器—融合—双任务头的整体架构。

4. Zhuang Y, Liu M, Bai W, et al. CMAD: Correlation-Aware and Modalities-Aware Distillation for Multimodal Sentiment Analysis with Missing Modalities[C]//ICCV. 2025.
   - 缺失模态下的模态感知蒸馏，支撑缺失鲁棒训练策略与模态权重设计。

5. Zhu A, Hu M, Wang X, et al. Proxy-Driven Robust Multimodal Sentiment Analysis with Incomplete Data[C]//ACL. 2025.
   - 不完整数据鲁棒多模态情感分析，支撑代理驱动的缺失处理思路。

6. Mai S, Han S. Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective[C]//ACL. 2026.
   - 因果推断视角的不变模态表示学习，支撑模态级注意力与不变性设计。

7. Fang Y, Huang W, Wan G, et al. EMOE: Modality-Specific Enhanced Dynamic Emotion Experts[C]//CVPR. 2025.
   - 模态特定增强动态情感专家，支撑单模态编码器与门控融合设计。

8. Wan J, Gong C, Fu G. Locate and Explain: Joint Multimodal Emotion Cause Extraction and Summarization in Conversation[C]//ACL. 2026.
   - 多模态情感原因定位与解释，支撑 Q3 片段级关键证据定位方法。

9. Mai S, Han S. CaReFlow: Cyclic Adaptive Rectified Flow for Multimodal Fusion[C]//CVPR. 2026.
   - 多模态融合的循环自适应流方法，支撑融合层设计参考。

10. 王楠，王淇，欧阳丹彤. 基于知识蒸馏与动态调整机制的多模态情感分析模型[J]. 计算机学报, 2025, 48(8): 1923-1942.
    - 中文多模态情感分析方法，支撑知识蒸馏与动态调整思路的中文文献依据。

### 补充方法学文献（用于工具与基础方法说明）

11. Devlin J, Chang M W, Lee K, et al. BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding[C]//NAACL. 2019. DOI: 10.18653/v1/N19-1423.
    - BERT 文本特征提取的基础方法依据。

12. Zadeh A, Chen M, Poria S, et al. Tensor Fusion Network for Multimodal Sentiment Analysis[C]//EMNLP. 2017. DOI: 10.18653/v1/D17-1115.
    - 多模态情感分析双任务（分类+回归）建模范式的经典参考。

13. Selvaraju R R, Cogswell M, Das A, et al. Grad-CAM: Visual Explanations from Deep Networks via Gradient-based Localization[C]//ICCV. 2017. DOI: 10.1109/ICCV.2017.74.
    - 可解释性方法参考，支撑注意力/遮挡验证设计。

14. Eyben F, Weninger F, Gross F, et al. Recent developments in openSMILE, the Munich open-source multimedia feature extractor[C]//ACM MM. 2013. DOI: 10.1145/2502081.2502224.
    - 语音韵律特征提取参考（librosa 实现等价功能）。
